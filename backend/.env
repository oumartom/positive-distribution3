# ─────────────────────────────────────────
#  POSITIVE DISTRIBUTION — Configuration
#  Ces variables sont injectées par Railway
#  automatiquement — ne pas modifier
# ─────────────────────────────────────────

# Railway injecte ces variables MySQL automatiquement :
# MYSQLHOST, MYSQLPORT, MYSQLUSER, MYSQLPASSWORD, MYSQLDATABASE

# Fallback local (développement)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=oumartom
DB_NAME=positive_distribution

# Serveur
PORT=3001
NODE_ENV=production

# JWT
JWT_SECRET=PositiveDistrib_2026_SuperSecret_!@#
JWT_EXPIRES_IN=12h
